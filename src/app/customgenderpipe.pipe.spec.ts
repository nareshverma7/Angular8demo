import { CustomgenderpipePipe } from './customgenderpipe.pipe';

describe('CustomgenderpipePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomgenderpipePipe();
    expect(pipe).toBeTruthy();
  });
});
